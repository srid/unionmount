builds:
  include:
    # Their M1 builder is so slow that we need higher delay in tests
    # - "packages.aarch64-darwin.default"
    - "packages.x86_64-linux.default"
  exclude:
    - "devShells.*.*"
    - "checks.*.*"
